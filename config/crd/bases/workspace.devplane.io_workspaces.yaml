# Generated CRD for Workspace. Run 'make manifests' to regenerate.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: workspaces.workspace.devplane.io
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
spec:
  group: workspace.devplane.io
  names:
    kind: Workspace
    listKind: WorkspaceList
    plural: workspaces
    singular: workspace
    shortNames:
    - ws
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    storage: true
    subresources:
      status: {}
    schema:
      openAPIV3Schema:
        description: Workspace is the Schema for the workspaces API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: WorkspaceSpec defines the desired state of a Workspace.
            properties:
              aiConfig:
                description: AIConfig configures the AI coding assistant (vLLM endpoint,
                  model).
                properties:
                  defaultProvider:
                    type: string
                  vllmEndpoint:
                    type: string
                  vllmModel:
                    type: string
                required:
                - vllmEndpoint
                - vllmModel
                type: object
              persistence:
                description: Persistence configures storage class for the workspace
                  PVC.
                properties:
                  storageClass:
                    type: string
                type: object
              resources:
                description: Resources defines CPU, memory, and storage for the workspace
                  pod.
                properties:
                  cpu:
                    type: string
                  memory:
                    type: string
                  storage:
                    type: string
                required:
                - cpu
                - memory
                - storage
                type: object
              user:
                description: User identifies the workspace owner (from OIDC).
                properties:
                  email:
                    type: string
                  id:
                    type: string
                required:
                - email
                - id
                type: object
            required:
            - aiConfig
            - persistence
            - resources
            - user
            type: object
          status:
            description: WorkspaceStatus defines the observed state of a Workspace.
            properties:
              lastAccessed:
                format: date-time
                type: string
              message:
                description: Human-readable error or info (e.g. validation failure, PVC not bound).
                type: string
              phase:
                type: string
              podName:
                type: string
              serviceEndpoint:
                type: string
            type: object
        type: object
    additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.user.id
      name: User
      type: string
    - jsonPath: .status.podName
      name: Pod
      type: string
